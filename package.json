{
  "name": "godot-profile-viewer",
  "version": "0.1.0",
  "description": "Local web service that renders a Decentraland profile via the godot-explorer Docker image.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
